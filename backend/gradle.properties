# Retry transient dependency-repository failures (e.g. Maven Central 429s seen
# consistently from the cloud pipeline's egress proxy) with backoff, instead of
# failing the whole build on the first rate-limited request.
systemProp.org.gradle.internal.repository.max.tentatives=5
systemProp.org.gradle.internal.repository.initial.backoff=1000
