# Production environment variables for frontend (Render Static Site)
# Copy this file to .env.production and set the actual values
# .env.production is gitignored and should never be committed

# API base URL — set to your backend API domain
# In Render, this will be injected from the dashboard
VITE_API_URL=https://api.yourshop.in
